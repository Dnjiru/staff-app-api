{{#partial "content"}}
    {{#if editTask}}
        <h1>Edit this News!</h1>
        <form action="/newss/{{news.id}}" method="post">
            <br>
            <label for="description">Edit this News's description</label>
            <input id="description" name="description" type="text" value="{{news.description}}">
            <br>
            <label for="departmentId">Update this News's Department:</label>
            <select id="departmentId" name="departmentId">
                {{#each departments}}
                    <option value="{{id}}">{{name}}</option>
                {{/each}}
            </select>
            <p>Don't see the right Department?<a href="/departments/new">Add a new one first.</a></p>
            <button type="submit" class="btn btn-default">Add!</button>
        </form>
    {{else}}
        <h2>Add a new News!</h2>
        <form action="/newss" method="post">
            {{#if departments}}
                <label for="description">News Description</label>
                <input id="description" name="description" type="text">
                <label for="departmentId">Categorize this News as:</label>
                <select id="departmentId" name="departmentId">
                    {{#each departments}}
                        <option value="{{id}}">{{name}}</option>
                    {{/each}}
                </select>
                <p>Don't see the right Department?<a href="/departments/new">Add a new one first.</a></p>
            {{else}}
                <h3> You'll need to add a Department before you add a News. <a href="/departments/new">Add one here.</a> </h3>
            {{/if}}
            <button type="submit" class="btn btn-secondary">Add!</button>
        </form>
    {{/if}}
{{/partial}}

{{> layout.hbs}}
